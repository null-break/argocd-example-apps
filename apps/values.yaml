config:
  spec:
    destination:
      server: https://kubernetes.default.svc
    source:
      repoURL: https://github.com/null-break/argocd-example-apps
      targetRevision: master

applications:
  - name: helm-sample-1
    namespace: argo-cd-sample
    tool:
      helm:
        releaseName: helm-sample-1
  - name: helm-sample-2
    namespace: argo-cd-sample
    tool:
      helm:
        releaseName: helm-sample-2
